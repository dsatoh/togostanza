{{#if params}}
<{{tagName}}
  {{#each params as |param|}}
  {{param.name}}{{#unless (eq param.name param.value)}}="{{param.value}}"{{/unless}}
  {{/each}}
></{{tagName}}>
{{else}}
<{{tagName}}></{{tagName}}>
{{/if}}
